<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastrar passageiro</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <h1> Cadastrar Passageiro </h1><br>
        <a class="btn btn-danger" href="/">Voltar</a><br><br>
        <% if (jaCadastrado) { %>
            <div class="alert alert-danger" role="alert">E-mail já cadastrado!</div>
        <% } %>
        <form action= "/passageiro" method="post">
            <input type="hidden" name="id" value="<%= passageiro.id %>">
            <div <%= escondido %> class="mb-3">
                <label class="form-label">E-mail</label>
                <input required type="email" value="<%= passageiro.email %>" required class="form-control" name="email">
            </div>
            <div class="mb-3">
                <label class="form-label">Nome</label>
                <input type="text" value="<%= passageiro.nome %>" required class="form-control" name="nome">
            </div>
            <div class="mb-3">
                <label class="form-label">Senha</label>
                <input type="password" required class="form-control" name="senha">
            </div>
            <button type="submit" class="btn btn-success">Salvar</button>
        </form>
    </div>
</body>
</html>